卷积定理

其实就是把多项式运算统一封装成向量的形式，在一些问题上会很有用。

假设有两个多项式f,g

F,G分别表示f,g的系数向量，


$FXGX = (F@ G)X，X=[x^1,x^2,...x^n]^T$

个人更想将X写成J，J表示进制j下的一串数字F实际表示的大小是多少，此时变动的是进制


和其他运算符的运算律，这里不想算了，因为后续会用到的运算符号太多了：逐点乘、矩阵乘、加法、左移右移......不如需要的时候再去推。